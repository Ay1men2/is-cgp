# IS-CGP — 智安对话治理平台

**Intelligent Secure Conversation Governance Platform**

IS-CGP 是面向内网/私有化部署的大模型对话治理中间层，目标是让**上下文进入模型的路径可控、可解释、可回放**。

---

## 1) 项目定位（Positioning）

- **治理中间层**：位于业务系统与模型推理后端之间，负责上下文治理，而非推理本身。
- **可审计执行单元**：所有上下文选择、过滤、截断、降级决策都可落库回放。
- **工程化 RLM**：以确定性、可治理为优先，强调可解释性而非生成式“聪明”。

> 关键词：**Governance Middleware / On-Prem / RLM Pipeline / Auditable Context Assembly**

---

## 2) 我们解决的问题（Why）

当组织内部大模型被多项目、多团队、多人长期使用时，问题会从“推理能力”转向“治理能力”：

- 资产与上下文**跨项目污染**
- 权限与策略变化后**难以回溯**
- 缺乏**清晰的证据链**与问责路径
- 长上下文下**成本失控、质量波动**

IS-CGP 提供一个独立、可解释、可审计的治理层，确保“上下文为何进入”可被证明与追踪。

---

## 3) IS-CGP 不是什么（Boundary）

- ❌ 不提供模型推理能力（只治理，不推理）
- ❌ 不提供聊天 UI 或前端产品
- ❌ 不是 Agent / AutoGPT 框架
- ❌ 不是公网 SaaS

---

## 4) 核心能力（Capability Snapshot）

- **会话 / 项目级隔离**：减少上下文串扰
- **RLM 治理流水线**：检索 → 过滤 → 排序 → 截断 → 裁决
- **审计与回放**：每次决策记录在 `rlm_runs`
- **长上下文治理**：预算控制、策略约束、可降级 fallback

---

## 5) 系统架构（Architecture）

```
业务系统 / 现有聊天 UI
        ↓
      IS-CGP
  (治理流水线)
        ↓
  模型推理后端
 (vLLM / llama.cpp / others)
```

IS-CGP 与推理引擎完全解耦，适配器可替换。

---

## 6) RLM 工程路径（当前设计）

> 目标：两分钟 Demo 能演示 `Plan → Examine → Decision ×3 → Assemble`，并落库回放。

**核心对象（简化）**
- `artifacts`：治理输入资产（doc/code/note/cache 等）
- `rlm_runs`：每次治理运行的证据链（options / rounds / errors / assembled_context）

**主流程（概念）**
1. **Retrieval**：从 session/project/global 范围召回候选
2. **Examine**：按计划抽取片段（head/range/grep）并缓存
3. **Decision**：三轮裁决与预算控制
4. **Assemble**：生成最终上下文 + 可选 prompt

---

## 7) 当前阶段状态（Project Status）

🚧 **Active Development — 内部工程验证阶段**

### 已完成
- FastAPI 服务骨架 + `/healthz`
- PostgreSQL + Redis 依赖
- Alembic 迁移体系
- 基础项目/会话 API
- `artifacts` / `rlm_runs` 表结构落地

### 进行中（v0.2–v0.3）
- Candidate 召回质量提升（type 白名单 + token 化）
- RLM 运行写入与审计链完善
- Examine + Redis cache
- LLM Judge 规划/裁决协议
- 三轮闭环 Assemble

---

## 8) 路线图（Roadmap）

- **v0.2**：RLM MVP（召回 + 三轮裁决 + 回放）
- **v0.3**：缓存治理与降级容错
- **v1.0**：治理能力产品化（控制台 + 推理适配器 + 运维手册）

---

## 9) 附录

- RLM 工程范围说明：`docs/rlm_scope.md`

---

## Quickstart (Local)

1. `cp .env.example .env`
2. `docker compose -f infra/docker-compose.yml up -d --build`
3. `docker compose -f infra/docker-compose.yml run --rm demo`
4. `docker compose -f infra/docker-compose.yml down -v`

---

## License

Apache License 2.0

---

## 名称声明

“IS-CGP（Intelligent Secure Conversation Governance Platform）”为项目名称。
Fork 或衍生项目不得在未授权情况下使用该名称暗示官方关系。
